function card(point,suit){
    if (point > 0 && point < 14){
        this.point = point;
    }
   
    if (suit === "diamonds" || "clubs" || "spades" || "hearts")

this.point = point;
this.suit = suit;
this.getImageUrl = getImage;


}

var aCard = new card(5,"hearts")
console.log(aCard);